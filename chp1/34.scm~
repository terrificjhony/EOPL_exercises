(define (path n bst)
  (if (null? bst)
      '()
      (cond ((= n (car bst))
	     '())
	    ((< n (car bst))
	     (cons 'left (path n (cadr bst))))
	    ((> n (car bst))
	     (cons 'left (path n (cddr bst))))
